# Hackaton Decathlon Amilia

# Steps

- 1 Pull this image from dockerhub
```sh
docker pull nickgryg/alpine-pandas
```

- 2 Build the docker image and run the container
```sh
docker-compose up --build -d
```